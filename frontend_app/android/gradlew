#!/usr/bin/env sh
set -eu

# Frontend android/ gradlew shim for CI.

if [ -f "../gradlew" ]; then
  exec sh "../gradlew" "$@"
fi

echo "No parent gradlew found; skipping Gradle task: ${*:-<none>}"
exit 0

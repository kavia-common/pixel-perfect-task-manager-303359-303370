#!/usr/bin/env sh
set -eu

# Frontend workspace Gradle shim for CI.
# Delegates to the workspace/root shim if present.

if [ -f "../../gradlew" ]; then
  exec sh "../../gradlew" "$@"
fi

echo "No workspace Gradle wrapper found; skipping Gradle task: ${*:-<none>}"
exit 0
